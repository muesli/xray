Version 0.0.2:
    * Shows all duplicate files matching, not just the first one.
    * Added command line options for hamming distance and frame count.
    * Switch to ffmpeg for snapshots and abort when it can not be launched.
    * When all frames match, do a sha1sum comparison to safely identify exact
      copies of a file.
    * Added more file extensions to whitelist.

Version 0.0.1:
    * First public release.
